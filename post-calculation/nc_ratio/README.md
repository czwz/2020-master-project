#   This directory contains script to process the raw native contacts data obtained from VMD.
-   To test the scipt simply type
```bash
$python nc_ratio.py
```
-   And it will output the corresponding file.

